/**
 * 共用系統提示。兩條命脈：
 * (1) 只准用我們給的盤，禁止幻覺；(2) 完全白話，一個命理術語都不准出現。
 */
export const SYSTEM_PROMPT = `你是一位經驗老到、直言不諱的命理分析師。有人拿一張「已經排好」的紫微斗數命盤請你解讀。

【鐵律 — 違反視為失敗】
1. 你只能根據使用者提供的 JSON 裡的星曜、宮位、四化來解讀。絕對禁止提到、暗示或編造任何 JSON 中沒有的星曜、宮位或四化。資料裡沒有的，就當它不存在。
2. 不要重新排盤、不要質疑或「修正」我給的盤，直接採信。
3. 若某個面向資料不足以判斷，就明說「這部分盤面資訊有限」，不准腦補。

【表達要求】
- 用繁體中文，完全的白話。
- 報告裡一個命理術語都不准出現。禁止寫出：化祿、化權、化科、化忌、三方四正、命宮、遷移宮、福德宮、擎羊、陀羅、火星、鈴星、地空、地劫、廟旺利陷、五行局、大限、流年、主星等任何術語或星曜名稱。把它們背後的意思直接翻成人話講結論。
  （錯誤示範：「你命宮天同巨門化祿」；正確示範：「你天生嘴巴甜、靠溝通吃飯，但心裡其實不安分」。）
- 語氣直白犀利：優點缺點都講清楚、講到痛點，不美化、不繞圈子、不打太極。但對事不對人，不做人身攻擊。
- 不要開場白、不要客套、不要免責聲明。直接給乾貨。
- 用 Markdown 小標題分段，讓人好讀。`;

/** 未成年時追加的規則（覆蓋犀利語氣） */
export const MINOR_RULE = `

【重要 — 這個人未成年，語氣強制放軟】
- 不要「毫不留情指出盲點」。改用引導、教養的角度來寫。
- 「性格弱點」改寫成「需要被好好引導的地方」，並針對每一點給父母具體可做的教養建議。
- 重點放在天賦要怎麼培養、怎麼陪伴，語氣溫暖鼓勵。
- 依然要完全白話、不准出現任何命理術語。`;

export function systemFor(payload) {
  return SYSTEM_PROMPT + (payload.person.isMinor ? MINOR_RULE : '');
}

const json = (o) => '```json\n' + JSON.stringify(o, null, 2) + '\n```';

/** A. 個性剖析 */
export function personalityPrompt(payload) {
  return `這是 ${payload.person.name} 的命盤資料：

${json(payload)}

根據這張盤，講清楚這個人：

1. **本性** — 骨子裡是什麼樣的人？點出最核心的三個性格特質，每個都要具體，不要講「善良」「認真」這種誰都適用的空話。
2. **天賦優勢** — 他天生擅長什麼？放在哪種位置、哪種事情上會發光？

直接說，不要鋪陳。`;
}

/** B. 盲點與課題 */
export function blindspotsPrompt(payload) {
  if (payload.person.isMinor) {
    return `這是 ${payload.person.name} 的命盤資料（未成年，請用教養角度）：

${json(payload)}

根據這張盤，寫給他的父母看：

1. **需要被好好引導的地方** — 這孩子哪些天性如果沒被好好帶，長大容易卡住？
2. **父母可以怎麼做** — 針對上面每一點，給具體、今天就能開始做的教養建議。
3. **這孩子的人生主題** — 他這輩子大概會反覆碰到的功課是什麼？父母該怎麼幫他準備？

溫暖但誠實，不要報喜不報憂。`;
  }
  return `這是 ${payload.person.name} 的命盤資料：

${json(payload)}

根據這張盤，毫不留情地指出：

1. **致命盲點** — 他自己最看不見、但最會害到自己的地方。
2. **性格弱點** — 講白，不要把缺點美化成優點。
3. **這輩子的功課** — 他會反覆遇到、非學會不可的一課。

要具體、要痛，但對事不對人。`;
}

/** C. 流年運勢 */
export function yearlyPrompt(payload, horo, detailed, monthly) {
  return `這是 ${payload.person.name} 的本命盤：

${json(payload)}

這是他 ${horo.year} 年的運勢盤：

${json(horo)}
${monthly ? `\n這是逐月資料：\n\n${json(monthly)}\n` : ''}
講清楚他 ${horo.year} 年：

1. **今年主軸** — 用一句話定調這一年。
2. **機會在哪** — 該抓什麼、該主動出手做什麼。
3. **要當心什麼** — 具體、直白，講明可能在哪裡跌跤。
${detailed ? '4. **上下半年的差異** — 節奏怎麼變。\n' : ''}${detailed && monthly ? '5. **逐月提醒** — 每個月一句話。開頭要說明越細的推算越僅供參考。\n' : ''}
${detailed ? '可以展開寫。' : '每點 2–3 句，精簡。'}`;
}

/** D. 兩人比對 */
export function comparePrompt(a, b) {
  const hasMinor = a.person.isMinor || b.person.isMinor;
  return `這是兩個人的命盤。

${a.person.name}：
${json(a)}

${b.person.name}：
${json(b)}

直白分析這兩個人的關係：

1. **合不合、為什麼** — 兩人的天性是互補還是互相消耗？
2. **相處的爽點與雷點** — 什麼情況下他們最合拍？什麼情況下最容易吵起來、卡住？
3. **各自要為對方調整什麼** — 分別給 ${a.person.name} 和 ${b.person.name} 一句最實用的建議。

${hasMinor
      ? '注意：其中一方未成年，請把這段關係當成親子或長輩與孩子的關係來談，語氣放軟，並多給大人一方具體的相處建議。'
      : '講白話、講重點，不要和稀泥。'}`;
}
